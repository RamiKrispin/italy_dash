name: Data Refresh

on: [push]
  #schedule:  
    # * is a special character in YAML so you have to quote this string
   # - cron:  '0 */6 * * *'
jobs:
  build:
    name: refresh the dashboard
    runs-on: ubuntu-18.04 
    container: 
     image: rkrispin/covid19italy:dev
    steps:
      - name: checkout_repo
        uses: actions/checkout@v2
        with: 
          ref: 'master'
      - name: Install dependencies
        run: Rscript -e "devtools::install_github('RamiKrispin/covid19Italy')" -e "devtools::install_github('RamiKrispin/coronavirus')"
      - name: Render the site
        run: Rscript -e "rmarkdown::render_site()"
      - name: Push updates  
        if: success()
        run: |
          #if [[ "$(git status --porcelain)" != "" ]]; then
              git config --global user.name 'RamiKrispin'
              git config --global user.email 'ramkrisp@umich.edu'
             # git commit -am "Update the dashboard"
             # git push
          #fi
      
